import Router, { redirect, queries, params, urls, logs } from "./core/index.js";
import { Switch, Scope, Route, Title, View, Link } from "./ui/index.js";

export default Router;
export { Switch, Scope, Route, Title, View, Link, redirect, queries, params, urls, logs }